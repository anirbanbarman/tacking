import { ShowDropdownDirective } from './show-dropdown.directive';

describe('ShowDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
